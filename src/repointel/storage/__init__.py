"""Persistence layer for repository memory (Phase 4).

Subpackages:
- ``json`` — the ``.repointel/`` JSON files (repo/graph/architecture/...).
- ``sqlite`` — indexed store for fast queries and incremental updates (Phase 7).
"""
