"""Context layer — turns raw structure into agent-consumable understanding.

Subpackages:
- ``architecture`` — detect architectural style (Phase 1 heuristic; deepens in Phase 6).
- ``summary`` — human/agent-readable explanations of modules (Phase 8).
- ``compression`` — squeeze repository memory into a small token budget (Phase 4/5).
"""
